#include "main.h"
/**
 * add - it returns a sum of its parameter
 * Description: a= int type number, b= int type number
 * Return: Always 0
 */
int add(int a, int b)

	a = int a
	b = int b

{
	return (a + b);
}
