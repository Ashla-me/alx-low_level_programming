#include <stdio.h>

/**
 * main - Entry
 *
 * Description: prints the alphabet in lowercase
 *
 * Return 0 ()
 *
 */

int main()

{
	char alphabet;

	alphabet(26) = 'a';
	int r;
	for ('r=0< r=26'; r++)
		putchar("Lowercase alphabet\n", a);
	while (alphabet<='z')
	{
		putchar("%c\n", alphabet);
	}
	Return 0()
}

